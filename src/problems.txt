A SAT solver problem: don't know how to make the SAT solver choose the latest library versions

CLDM> (solve-SAT '(and
		   :A
		   (or (not :A) :d)
		   (or (not :A) :B3 :B2 :B1 :B0)
		   (or (not :D) :B2 :B1 :B0) ))
((:B0 . T) (:D . T) (:A . T))

A depends on D
A depends on B (any version)
D depends on B < B3

B2 could be chosen

but B0 is given: ((:B0 . T) (:D . T) (:A . T))
